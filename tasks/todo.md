# Local OCR GUI Todo

## Plan

- [x] 确认目标约束：离线可用、桌面 GUI、可直接构建分发
- [x] 选定技术栈：Electron + React + TypeScript + PP-OCRv5
- [x] 保留并复用现有 React OCR 界面
- [x] 改造前端构建，适配 `file://` 加载
- [x] 接入 Electron 主进程、预加载脚本和桌面打包配置
- [x] 将 OCR 引擎从 `Tesseract.js` 迁移到本地 `PP-OCRv5`
- [x] 验证新的 OCR 引擎与桌面构建
- [x] 补充强引擎的分发说明和已知限制
- [x] 为批量识别增加“停止队列”能力，避免长队列时只能被动等待
- [x] 为单任务和批量任务补充更明确的执行状态反馈，区分运行中、停止中、已停止
- [x] 验证停止队列改动不会破坏现有前端生产构建
- [x] 增加系统剪贴板图片读取能力，支持直接粘贴截图进入任务列表
- [x] 补充桌面快捷键与显式按钮，让用户可通过 `Ctrl/Cmd + V` 或按钮导入剪贴板图片
- [x] 验证剪贴板导入改动不会破坏现有前端和 Electron 构建
- [x] 为剪贴板截图增加自动识别开关，并持久化用户偏好
- [x] 补充粘贴截图后的自动识别状态反馈，覆盖开启、关闭、自检未通过三类场景
- [x] 验证截图自动识别改动不会破坏现有前端和 Electron 构建
- [x] 为最近识别结果增加本地持久化，支持重开应用后恢复任务列表
- [x] 补充恢复后的预览与任务区提示，保证历史任务仍可查看文本和框选明细
- [x] 验证最近结果恢复改动不会破坏现有前端和 Electron 构建
- [x] 增加“清空最近历史”动作，支持一键移除非固定保留的历史任务
- [x] 为任务增加“固定保留”状态，保证重要结果不受最近条数限制和清空历史影响
- [x] 验证历史管理改动不会破坏现有前端和 Electron 构建
- [x] 增加批量识别完成桌面通知，便于切走窗口后及时获知结果
- [x] 为导出流程记录上次导出目录，并增加一键打开入口
- [x] 验证通知与导出目录改动不会破坏现有前端和 Electron 构建
- [x] 为结果区增加搜索状态、匹配计数和上下跳转能力
- [x] 为预览框、块列表和结果文本增加搜索命中高亮联动
- [x] 验证结果搜索与高亮改动不会破坏现有前端和 Electron 构建
- [x] 为导出流程增加最近记录持久化，保留最近几次导出文件
- [x] 在结果区增加导出记录列表，支持打开文件、打开所在目录和清空记录
- [x] 验证导出记录列表改动不会破坏现有前端和 Electron 构建
- [x] 为任务增加 OCR 原始结果基线，支持编辑后和原始输出对比
- [x] 在结果区增加对比模式、差异摘要和前后文本视图
- [x] 验证结果对比模式改动不会破坏现有前端和 Electron 构建
- [x] 为导出流程增加命名模板，支持固定导出文件命名规则
- [x] 为导出流程增加默认导出目录设置，并写入保存对话框默认路径
- [x] 验证导出模板与默认目录改动不会破坏现有前端和 Electron 构建

## Review

- `npm run prepare:ocr-models` 通过，`vendor/ocr-models` 已落地 `ch_PP-OCRv5_server_det.onnx`、`ch_PP-OCRv5_rec_server_infer.onnx`、`ppocrv5_dict.txt`
- 已增加 `en_PP-OCRv5_rec_mobile_infer.onnx`、`ppocrv5_en_dict.txt` 和英文词典纠错，用于英文增强分支
- `node ./scripts/smoke-ocr.mjs <image>` 可运行，样例图过滤后输出 `离线OCR工作台 / Hello Windows Native`
- 已增加前端“结果明细”面板，展示文本块的 `source`、`confidence` 和 `box`
- 已增加用户词库编辑与本地持久化，保存后重新识别即可参与英文纠错
- 已增加预览图 OCR 框覆盖层，并可和右侧结果明细联动高亮
- 已增加 TXT / JSON 导出入口，走桌面保存对话框
- JSON 导出已补充应用信息、图片元数据、预览缩放信息、文本块列表和用户词库快照
- 已增加预览框点击驱动明细滚动定位
- 已增加块级文本编辑，并同步回写整体文本和导出结果
- 已补齐批量任务界面所需的 `App.css` 类名，任务筛选、任务卡片、预览覆盖层和块编辑区样式已落地
- 已增加首启环境自检面板，展示运行形态、平台架构、Electron 版本、模型目录、用户词库路径和模型文件完整性
- 已增加 OCR 引擎预热检查，启动后可直接确认模型、词典和 ONNX Runtime 是否就绪
- 已增加首启恢复动作，可直接打开模型目录、用户数据目录、文档目录，复制或导出诊断信息
- 已增加用户词库损坏识别、目录可写性检查和“一键重建空词库”恢复入口
- 已增加首次使用引导面板，空任务时会展示 4 步上手流程和首张图片 CTA
- 已增加 Windows 安装提示，覆盖 setup、portable、SmartScreen 和杀毒软件隔离场景
- 已增加运行时生成的内置示例图，并提供“只载入示例图”和“一键试跑示例图”入口
- 已增加示例图自动验收卡，会按预设中英文本给出通过线和缺失项
- 已将批量识别收敛为“只处理待处理和失败任务”，避免批量重跑覆盖已完成结果
- 已增加“停止队列”按钮，停止请求会在当前图片完成后生效，并保留剩余任务待下次继续
- 已增加队列执行摘要，明确展示运行中、停止中、已停止、成功数、失败数和剩余数
- 已在队列运行期间锁定添加图片、清空任务、删除任务、切换识别模式、保存词库和重建词库入口，避免并发状态互相覆盖
- 已在 Electron 预加载层暴露系统剪贴板读图接口，可直接读取截图或复制过来的图片
- 已增加“粘贴截图”按钮，并支持在非输入区按 `Ctrl/Cmd + V` 直接把剪贴板图片加入任务列表
- 已增加剪贴板导入中的状态提示与入口锁定，避免导入尚未结束时触发重复操作
- 已增加“粘贴截图后自动识别”开关，默认开启，并使用 `localStorage` 持久化用户偏好
- 已补充粘贴截图后的自动识别反馈，区分自动识别中、已关闭自动识别、自检进行中、自检失败四类提示
- 已为快捷键粘贴增加 `event.repeat` 防抖，避免长按 `Ctrl/Cmd + V` 连续触发重复导入
- 已增加最近识别结果持久化，当前会保存最近 6 条有结果或失败状态的任务，并在重开应用后自动恢复
- 已为历史任务预览增加压缩存储与恢复逻辑，恢复后的任务仍可查看预览、文本块、置信度和框选高亮
- 已将对象 URL 清理改为仅回收 `blob:` 预览，避免恢复后的 `data:` 历史预览被误释放
- 已增加“清空最近历史”按钮，可一键移除非固定保留的历史任务，固定保留任务不会被清理
- 已为任务增加“固定保留”状态，任务卡与结果区都可直接切换，固定任务会长期参与本地恢复
- 已将本地历史持久化规则改为“固定保留任务 + 最近 6 条非固定历史”，保证重要任务不受最近条数限制
- 已在 Electron 主进程增加桌面通知接口，批量识别完成或队列停止后会发送系统通知
- 已为导出流程记录上次导出目录，当前 TXT、JSON 和诊断导出都会更新该目录
- 已在结果区增加“打开上次导出目录”入口，重开应用后仍可继续使用最近导出目录
- 已在结果区增加搜索栏、匹配计数和上一处/下一处跳转，回车会跳到下一处，`Shift + Enter` 会跳到上一处
- 已为预览框和块列表增加搜索命中高亮，当前匹配会同步驱动右侧块定位
- 已为全文搜索增加文本区选择与滚动定位，在没有块级命中时仍可跳转到对应全文位置
- 已增加导出记录持久化，当前会保留最近 8 条导出文件记录，覆盖 TXT、JSON 和诊断导出
- 已在结果区增加导出记录列表，支持直接打开导出文件、打开所在目录和一键清空记录
- 已将导出记录与上次导出目录解耦，既保留快速打开目录入口，也能回看具体导出文件
- 已为任务增加 OCR 原始结果基线，重新识别后会更新基线，手动编辑和块级编辑都会基于该基线生成对比
- 已在结果区增加对比模式，展示原始 OCR 文本与当前文本的双栏差异视图
- 已增加差异摘要，当前可统计修改、新增、删除和字符差，便于快速检查编辑范围
- 已增加导出命名模板，当前支持 `{name}`、`{scope}`、`{kind}`、`{date}`、`{time}` 占位符，并自动补齐扩展名
- 已增加默认导出目录设置，可把上次导出目录设为固定默认目录，也可随时清除该设置
- 保存对话框已接入默认导出目录，当前 TXT、JSON 和诊断导出都会优先落到该默认目录
- `npm --prefix frontend run build` 通过，当前批量任务版 `App.tsx` 可完成 TypeScript 与 Vite 生产构建
- 已完成 `App.tsx` / `App.css` 类名比对，当前静态类名无缺失样式
- `npm run build` 通过，生成 2026-03-22 17:00:55 的 `release/mac-arm64/Local OCR Desk.app`
- `npm run build` 再次通过，生成 2026-03-23 16:03:21 的 `release/mac-arm64/Local OCR Desk.app`
- `npm run dist:win` 通过，生成 2026-03-22 17:02:04 的 `release/win-unpacked/Local OCR Desk.exe`
- `npm run dist:win` 通过，生成 2026-03-22 17:02:34 的 `release/Local OCR Desk-0.2.0-x64-setup.exe` 和 2026-03-22 17:02:36 的 `release/Local OCR Desk-0.2.0-x64-portable.exe`
- macOS 与 Windows 产物中均包含 `ocr-models` 目录，内置中文检测模型、中文识别模型、英文识别模型和双字典
- 已再次启动 `release/mac-arm64/Local OCR Desk.app`，进程可正常拉起，当前主进程 PID 为 `18093`
- 旧的 `Tesseract.js` 依赖、前端 wasm 资源、`frontend/*.traineddata` 和 `frontend/wailsjs` 已清理
